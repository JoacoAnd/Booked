import BookList from "@/components/BookList";
import { db } from "@/database/drizzle";
import React from "react";

const Page = async () => {
  return <>{/* <BookList title="Borrowed Books" books={sampleBooks} /> */}</>;
};

export default Page;
